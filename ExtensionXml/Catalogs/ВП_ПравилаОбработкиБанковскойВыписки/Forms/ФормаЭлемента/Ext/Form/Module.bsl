&НаКлиенте
Процедура ПриОткрытии(Отказ)

	УстановитьЗначенияПоУмолчанию();
	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура РежимПоискаСчетаПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура НаправлениеОперацииПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура РежимЗаполненияСчетаПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеВидОперацииПоступлениеПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеВидОперацииСписаниеПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеКонтрагентПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеДоговорКонтрагентаПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеБанковскийСчетКонтрагентаПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеСтатьяДвиженияДенежныхСредствПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеСчетДтПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеСчетКтПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеПодразделениеДтПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеПодразделениеКтПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура ДействиеСтатьяПрочихДоходовИРасходовПриИзменении(Элемент)

	ОбновитьДоступностьЭлементовФормы();

КонецПроцедуры

&НаКлиенте
Процедура УстановитьЗначенияПоУмолчанию()

	Если Не ЗначениеЗаполнено(Объект.НаправлениеОперации) Тогда
		Объект.НаправлениеОперации = ЗначениеПеречисления(
			"Перечисление.ВП_НаправленияОперацийВыписки.Любая");
	КонецЕсли;

	Если Не ЗначениеЗаполнено(Объект.РежимПоискаСчета) Тогда
		Объект.РежимПоискаСчета = ЗначениеПеречисления(
			"Перечисление.ВП_РежимыПоискаСчетаНаОплату.НеЗаполнять");
	КонецЕсли;

	Если Не ЗначениеЗаполнено(Объект.РежимЗаполненияСчета) Тогда
		Объект.РежимЗаполненияСчета = ЗначениеПеречисления(
			"Перечисление.ВП_РежимыЗаполненияСчетаНаОплату.Перезаписывать");
	КонецЕсли;

	ДействиеНеИзменять = ЗначениеПеречисления(
		"Перечисление.ВП_ВидыДействийЗаполненияРеквизитов.НеИзменять");
	ДействиеЗаполнить = ЗначениеПеречисления(
		"Перечисление.ВП_ВидыДействийЗаполненияРеквизитов.Заполнить");

	УстановитьДействиеПоУмолчанию("ДействиеВидОперацииПоступление", "ВидОперацииПоступление", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеВидОперацииСписание", "ВидОперацииСписание", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеКонтрагент", "ЗаполняемыйКонтрагент", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеДоговорКонтрагента", "ЗаполняемыйДоговорКонтрагента", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеБанковскийСчетКонтрагента", "ЗаполняемыйБанковскийСчетКонтрагента", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеСтатьяДвиженияДенежныхСредств", "СтатьяДвиженияДенежныхСредств", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеСчетДт", "СчетДт", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеСчетКт", "СчетКт", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеПодразделениеДт", "ПодразделениеДт", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеПодразделениеКт", "ПодразделениеКт", ДействиеНеИзменять, ДействиеЗаполнить);
	УстановитьДействиеПоУмолчанию("ДействиеСтатьяПрочихДоходовИРасходов", "СтатьяПрочихДоходовИРасходов", ДействиеНеИзменять, ДействиеЗаполнить);

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьДоступностьЭлементовФормы()

	ЭтоКонкретныйСчет = Объект.РежимПоискаСчета
		= ЗначениеПеречисления("Перечисление.ВП_РежимыПоискаСчетаНаОплату.КонкретныйСчет");
	ЭтоБезСчетаНаОплату = Объект.РежимПоискаСчета
		= ЗначениеПеречисления("Перечисление.ВП_РежимыПоискаСчетаНаОплату.НеЗаполнять");

	ЭтоПоступление = Объект.НаправлениеОперации
		= ЗначениеПеречисления("Перечисление.ВП_НаправленияОперацийВыписки.Поступление");
	ЭтоСписание = Объект.НаправлениеОперации
		= ЗначениеПеречисления("Перечисление.ВП_НаправленияОперацийВыписки.Списание");
	ЭтоЛюбаяОперация = Не ЗначениеЗаполнено(Объект.НаправлениеОперации)
		Или Объект.НаправлениеОперации
			= ЗначениеПеречисления("Перечисление.ВП_НаправленияОперацийВыписки.Любая");

	УстановитьДоступностьЭлемента("СчетНаОплатуПокупателю", ЭтоКонкретныйСчет);
	УстановитьВидимостьЭлемента("СчетНаОплатуПокупателю", Не ЭтоБезСчетаНаОплату);

	УстановитьДоступностьДействияИЗначения(
		"ДействиеВидОперацииПоступление",
		"ВидОперацииПоступление",
		ЭтоПоступление Или ЭтоЛюбаяОперация);
	УстановитьДоступностьДействияИЗначения(
		"ДействиеВидОперацииСписание",
		"ВидОперацииСписание",
		ЭтоСписание Или ЭтоЛюбаяОперация);

	УстановитьДоступностьДействияИЗначения("ДействиеКонтрагент", "ЗаполняемыйКонтрагент", Истина);
	УстановитьДоступностьДействияИЗначения("ДействиеДоговорКонтрагента", "ЗаполняемыйДоговорКонтрагента", Истина);
	УстановитьДоступностьДействияИЗначения("ДействиеБанковскийСчетКонтрагента", "ЗаполняемыйБанковскийСчетКонтрагента", Истина);
	УстановитьДоступностьДействияИЗначения("ДействиеСтатьяДвиженияДенежныхСредств", "СтатьяДвиженияДенежныхСредств", Истина);
	УстановитьДоступностьДействияИЗначения("ДействиеСтатьяПрочихДоходовИРасходов", "СтатьяПрочихДоходовИРасходов", Истина);

	УстановитьДоступностьДействияИЗначения(
		"ДействиеСчетКт",
		"СчетКт",
		ЭтоПоступление Или ЭтоЛюбаяОперация);
	УстановитьДоступностьДействияИЗначения(
		"ДействиеПодразделениеКт",
		"ПодразделениеКт",
		ЭтоПоступление Или ЭтоЛюбаяОперация);
	УстановитьДоступностьДействияИЗначения(
		"ДействиеСчетДт",
		"СчетДт",
		ЭтоСписание Или ЭтоЛюбаяОперация);
	УстановитьДоступностьДействияИЗначения(
		"ДействиеПодразделениеДт",
		"ПодразделениеДт",
		ЭтоСписание Или ЭтоЛюбаяОперация);

КонецПроцедуры

&НаКлиенте
Процедура УстановитьДействиеПоУмолчанию(ИмяДействия, ИмяЗначения, ДействиеНеИзменять, ДействиеЗаполнить)

	Попытка
		Если Не ЗначениеЗаполнено(Объект[ИмяДействия]) Тогда
			Объект[ИмяДействия] = ?(
				ЗначениеЗаполнено(Объект[ИмяЗначения]),
				ДействиеЗаполнить,
				ДействиеНеИзменять);
		КонецЕсли;
	Исключение
	КонецПопытки;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьДоступностьДействияИЗначения(ИмяДействия, ИмяЗначения, ДоступноПоНаправлению)

	УстановитьДоступностьЭлемента(ИмяДействия, ДоступноПоНаправлению);
	УстановитьДоступностьЭлемента(
		ИмяЗначения,
		ДоступноПоНаправлению И ДействиеЗаполнить(ИмяДействия));

КонецПроцедуры

&НаКлиенте
Функция ДействиеЗаполнить(ИмяДействия)

	Попытка
		Возврат Объект[ИмяДействия]
			= ЗначениеПеречисления("Перечисление.ВП_ВидыДействийЗаполненияРеквизитов.Заполнить");
	Исключение
	КонецПопытки;

	Возврат Ложь;

КонецФункции

&НаКлиенте
Функция ЗначениеПеречисления(ПолноеИмяЗначения)

	Возврат ПредопределенноеЗначение(ПолноеИмяЗначения);

КонецФункции

&НаКлиенте
Процедура УстановитьДоступностьЭлемента(ИмяЭлемента, Доступность)

	ЭлементФормы = Элементы.Найти(ИмяЭлемента);
	Если ЭлементФормы <> Неопределено Тогда
		ЭлементФормы.Доступность = Доступность;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьВидимостьЭлемента(ИмяЭлемента, Видимость)

	ЭлементФормы = Элементы.Найти(ИмяЭлемента);
	Если ЭлементФормы <> Неопределено Тогда
		ЭлементФормы.Видимость = Видимость;
	КонецЕсли;

КонецПроцедуры
